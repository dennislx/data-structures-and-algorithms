
## add lecture 

```sh
git checkout -b lecture     # create a new local branch
git commit & push           # push to remote branch
git pull upstream lecture   # fetch lecture from upstream branch
```